run-mode:
  single-run:
    name: playground
    fileNamePattern: playground/*.json
  report:
      type: enqueuer
      filename: src/inceptionTest/singleRunInceptionReport.json

outputs:
    -
      type: standard-output

variables:
    httpPort: 23076
    enqueuerKey: 10

log-level: trace
