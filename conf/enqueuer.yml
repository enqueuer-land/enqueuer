run-mode:
  single-run:
    name: playground
    fileNamePattern: playground.json
    report:
        type: enqueuer
        filename: result.json
outputs:
  -
    type: file
    filenamePrefix: output_
    filenameExtension: json

variables:
    httpPort: 23076
    enqueuerKey: 10

log-level: info