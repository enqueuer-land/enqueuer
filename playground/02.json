{
  "requisitionVersion": "01.00.00",
  "timeout": 3000,
  "subscriptions": [
    {
      "type": "uds",
      "path": "/tmp/unix.sock",
      "onMessageReceived": "test['session variable retrieved'] = message.indexOf('someValue') >= 0; console.log('Message: ' + message); persistEnqueuerVariable('enqueuerKey', 10);",
      "timeout": 500
    }
  ],
  "startEvent": {
    "publisher": {
      "type": "uds",
      "path": "/tmp/unix.sock",
      "payload": "{{sessionKey}}"
    }
  }
}