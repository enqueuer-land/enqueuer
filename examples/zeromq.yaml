publishers:
-   type: custom
    module: protocols/publishers/zeromq.js
    address: <<zeroMqAddress>>
    payload: enqueuer zeroes
    topic: topic.enqueuer.virgs
    subscriptionTimeout: 1000
subscriptions:
-   type: custom
    module: protocols/subscriptions/zeromq.js
    timeout: 1000
    address: <<zeroMqAddress>>
    topic: topic.enqueuer.virgs
    onMessageReceived:
        assertions:
        -   name: topic
            expect: topic
            toBeEqualTo: `topic.enqueuer.virgs`
        -   name: payload
            expect: payload
            toBeEqualTo: `enqueuer zeroes`
        -   name: address
            expect: store.zeroMqAddress
            toBeEqualTo: `<<zeroMqAddress>>`
