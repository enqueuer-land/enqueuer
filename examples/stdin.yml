subscriptions:
  - name: subscription description
    id: anyIdTest
    type: standard-input
    timeout: 3000
    onMessageReceived:
      assertions:
        - name: payload
          expect: message
          toBeEqualTo: "'enqueuer standard-input payload'"
