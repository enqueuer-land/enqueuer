runnableVersion: 01.00.00
name: kafkaName
id: kafka
runnables:
- timeout: 3000
  name: RequisitionKafka
  subscriptions:
  - type: kafka
    name: kafkaSub
    client:
      kafkaHost: localhost:9093
    options:
      topic: enqueuer-topic-name
  startEvent:
    publisher:
      type: kafka
      name: kafkaPub
      client:
        kafkaHost: localhost:9093
      topic: enqueuer-topic-name
      payload: stompQueue
