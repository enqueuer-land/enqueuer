onFinish:
    assertions:
        -   name: Last one was executed
            expectToBeDefined: store.lastExecution
requisitions:
    -   requisitions:
            -   requisitions:
                    -   onFinish:
                            store:
                                lastExecution: `lastExecution`
