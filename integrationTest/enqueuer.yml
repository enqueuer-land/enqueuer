requisitions:
  run-mode:

    single-run:
      fileNamePattern: integrationTest/*.json
      output-file: singleRunOutput.json

outputs:
  -
    type: mqtt
    brokerAddress: mqtt://localhost:1883
    topic: enqueuer/output


log-level: debug