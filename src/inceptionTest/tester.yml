requisitions:
  run-mode:
    single-run:
      fileNamePattern: src/inceptionTest/*.enq
outputs:
  -
    type: mqtt
    brokerAddress: mqtt://localhost:1883
    topic: enqueuer/inception/tester/output/doesNotMatter

log-level: debug