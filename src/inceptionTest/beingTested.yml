requisitions:
  run-mode:
    daemon:
      -
        type: uds
        path: /tmp/inUnix.sock
outputs:
  -
    type: file
    filenamePrefix: src/inceptionTest/inner_test_
    filenameExtension: json
  -
    type: uds
    path: /tmp/outUnix.sock

log-level: trace