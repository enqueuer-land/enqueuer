requisitions:

  run-mode:
    daemon:
      -
        type: mqtt
        brokerAddress: mqtt://localhost
        topic: enqueuer/inception/beingTested/requisitionInput
outputs:
  -
    type: mqtt
    brokerAddress: mqtt://localhost:1883
    topic: enqueuer/inception/beingTested/output/doesNotMatter

log-level: debug